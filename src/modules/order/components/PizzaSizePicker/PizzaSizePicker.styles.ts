import { css } from '@emotion/react';

const pizzaSizeContainer = css({
  display: 'flex',
  justifyContent: 'space-between',
  margin: '24px 160px',
  backgroundColor: '#ececf3',
  borderRadius: '56px',
});

export default { pizzaSizeContainer };
